import React from 'react';

const AddCategory = () => {
    return (
        <div>
            <form>
                <select>
                    <option>Category</option>
                </select>
            </form>
        </div>
    );
};

export default AddCategory;